name: python-ci

on:
  push:
  pull_request:

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    defaults:
      run:
        working-directory: python
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@v5
      - name: Install package and test deps
        run: uv pip install --system -e .[dev]
      - name: Run unit tests
        run: pytest tests/unit
  fast-integration:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@v5
      - name: Install package and test deps
        run: cd python && uv pip install --system -e .[dev]
      - name: Run fast integration tests
        run: make test.integration.fast
  artifact-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v5
      - name: Build distribution artifacts
        run: uv build
      - name: Validate artifact metadata
        run: uvx twine check --strict dist/*
      - name: Validate sdist and wheel contents
        run: |
          python - <<'PY'
          import tarfile
          import zipfile
          from pathlib import Path

          dist_dir = Path("dist")
          sdists = sorted(dist_dir.glob("*.tar.gz"))
          wheels = sorted(dist_dir.glob("*.whl"))

          assert len(sdists) == 1, sdists
          assert len(wheels) == 1, wheels

          sdist = sdists[0]
          with tarfile.open(sdist) as tar:
              names = set(tar.getnames())

          root = sdist.name.removesuffix(".tar.gz")
          assert f"{root}/pyproject.toml" in names
          assert f"{root}/README.md" in names
          assert f"{root}/LICENSE" in names
          assert any(name.endswith("PKG-INFO") for name in names)

          wheel = wheels[0]
          with zipfile.ZipFile(wheel) as archive:
              wheel_names = set(archive.namelist())

          assert "databricks_agent_notebooks/__init__.py" in wheel_names
          assert "databricks_agent_notebooks/for_agents/README.md" in wheel_names
          assert any(name.startswith("databricks_agent_notebooks/for_agents/examples/") for name in wheel_names)
          assert any(name.endswith(".dist-info/METADATA") for name in wheel_names)
          assert any(name.endswith(".dist-info/entry_points.txt") for name in wheel_names)
          assert any(name.endswith("dist-info/licenses/LICENSE") for name in wheel_names)
          PY
      - name: Install built wheel into clean venv
        run: |
          uv venv .artifact-venv
          uv pip install --python .artifact-venv/bin/python dist/*.whl
      - name: Proof installed artifact offline installer flows
        run: |
          .artifact-venv/bin/agent-notebook help
          .artifact-venv/bin/python scripts/installed_artifact_installer_proof.py
      - name: Upload built artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: python/dist/*
          if-no-files-found: error
