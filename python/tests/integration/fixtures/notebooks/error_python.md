# Error Test — Python

Notebook with an intentional error for testing error reporting.

```python
print("before error")
```

```python
raise ValueError("intentional test error")
```

```python
print("after error - should not execute")
```
